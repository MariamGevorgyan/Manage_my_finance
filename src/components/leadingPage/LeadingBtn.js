import { Button } from 'reactstrap'

export default function LeadingBtn({ buttonLabel, className }) {

    return (
        <div>
            <Button className="LeadingBtn" >
                Get Start
            </Button>
        </div>
    )
}